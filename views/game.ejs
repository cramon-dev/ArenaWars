<!DOCTYPE html>
<html>
	<head>
	    <title>Arena Wars</title>
	    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
	    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
	    <link rel="stylesheet" type="text/css" href="../css/style.css"/>
	    <script src="../js/libs/three.js"></script>
	    <script src="../js/libs/physi.js"></script>
		<script src="../js/libs/stats.min.js"></script>
		<script src="../js/libs/dat.gui.min.js"></script>
		<script src="../js/libs/OrbitControls.js"></script>
		<script src="../js/libs/gamestate.js"></script>
		<script src="https://cdn.socket.io/socket.io-1.3.7.js"></script>
	</head>
	<body>
		<header>
			<nav class="navbar navbar-inverse">
				<div class="container">
					<div class="navbar-header">
						<a class="navbar-brand">Arena Wars</a>
					</div>
					<section class="collapse navbar-collapse">
						<ul class="nav navbar-nav">
					        <% if (user) { %>
					        	<li><a href="/signout">Sign Out</a></li>
					        <% } else { %>
					        	<li><a href="/signin">Sign In</a></li>
					        <% } %>
						</ul>
					</section>
				</div>
			</nav>
		</header>
        <% if (user) { %>
            <div class="alert alert-success"><%= user %></div>
        <% } else { %>
        	<div class="alert alert-danger">Your battle statistics will not be saved unless you register for an account.</div>
        <% } %>
		<div id="container">
			<div id="stats">
			</div>
			<% if (!user) { %>
				<div class="enterNameForm">
		        	<input type="text" id="usernameEntry" name="username" placeholder="Username" />
		        	<button onclick="initGame(usernameEntry)" /> Play
		        </div>
	        <% } %>
		<div id="inset">
		<script src="../js/game.js"></script>
	</body>
</html>